# Browser-side: used in fetch() inside fashion-classifier.tsx
# Leave empty to use the Next.js /api proxy (recommended for local dev)
# Set to full URL for production: https://your-backend.com
NEXT_PUBLIC_API_URL=

# Server-side only: used by next.config.mjs rewrites()
BACKEND_URL=http://localhost:8000
